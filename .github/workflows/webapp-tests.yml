name: Run jest tests
run-name: Automated jest tests and coverage

on:
  push:
    paths:
      - 'webapp/**'
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    paths:
      - 'webapp/**'
    branches:
      - main

jobs:
  test:
    name: test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x, 20.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn install
      - run: yarn test
